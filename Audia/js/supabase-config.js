window.AUDIA_SUPABASE = {
  url: "",
  anonKey: "",
  redirectTo: window.location.origin + "/entrar.html",
};

// Preencha apenas a URL do projeto e a ANON KEY do Supabase.
// Nunca coloque a service_role key no frontend.
